# Airflow Logs Investigation - Dependencies
#
# For the mock demo (mock_demo.py):
#   No external dependencies required - uses only Python standard library
#
# For real AWS integration (fetch_*.py scripts):
boto3>=1.34,<2.0
botocore>=1.43.98,<2.0
requests>=2.31,<3.0

# Optional: for better output formatting
rich>=13.0,<15.0

# Optional: for data analysis
pandas>=2.0,<3.0

# The upper bounds are deliberate: an unbounded range lets a future major
# release land silently on a fresh install, and "it worked last month" is not a
# reproducible environment. For a pinned install, compile a lockfile from this
# file with pip-tools:
#   pip-compile scripts/requirements.txt -o scripts/requirements.lock
